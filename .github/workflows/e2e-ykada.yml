name: e2e-ykada

on:
  push:
    branches:
      - main

jobs:
  e2e-test:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Run e2e ykada test
        run: |
          scripts/e2e_ykada.sh
